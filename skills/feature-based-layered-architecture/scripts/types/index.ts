/**
 * Supabase 生成型のエイリアス
 *
 * database.ts から必要な型をここで再エクスポート
 * 使用例: import type { ProductRow } from '@/types'
 */
import type { Database } from './database'

// テーブル行型のエイリアス
// 例: export type ProductRow = Database['public']['Tables']['products']['Row']

// Enum型のエイリアス
// 例: export type OrderStatus = Database['public']['Enums']['order_status']
